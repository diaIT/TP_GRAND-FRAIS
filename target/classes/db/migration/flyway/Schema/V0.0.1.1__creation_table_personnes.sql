CREATE TABLE personne (
                          id SERIAL PRIMARY KEY,
                          nom VARCHAR(225),
                          prenom VARCHAR(225),
                          age INTEGER
);
